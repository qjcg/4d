# Timer

A simple, lightweight CLI timer.

# Install

```sh
go get github.com/qjcg/timer
```

# Usage

Display countdown:

```sh
timer -c 5m
```

Display elapsed time:

```sh
timer
```

Return key exits.
